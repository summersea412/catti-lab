# Local CATTI package import

The supplied package was read locally only. All 11 HTML files were parsed from their saved bodies and produced nine new practical passage records. The 15 PDFs were read for metadata and rendered for page inspection, but this runtime has no PDF text extractor, so their正文 is recorded as an explicit extraction failure and was not fabricated from filenames.

No network fetch was performed during this import. No synthetic text was generated. Existing Registry records remain intact.
