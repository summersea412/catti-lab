import React from 'react';import{VOCABULARY_EXERCISES,GRAMMAR_EXERCISES,READING_PASSAGES,CLOZE_PASSAGES}from'../../features/comprehensive/comprehensiveDataset';const go=id=>window.dispatchEvent(new CustomEvent('catti:navigate',{detail:id}));
export default function ComprehensiveHubPage(){const items=[['vocabulary','词汇专项',`${VOCABULARY_EXERCISES.length} items`],['grammar','语法专项',`${GRAMMAR_EXERCISES.length} items`],['reading','阅读理解',`${READING_PASSAGES.length} passages · ${READING_PASSAGES.length*5} questions`],['cloze','完形 / 语言运用',`${CLOZE_PASSAGES.length} passages · ${CLOZE_PASSAGES.length*10} blanks`]];return <div className="page-hero"><div><p className="eyebrow">COMPREHENSIVE ABILITY</p><h2>综合能力</h2><p>这是 CATTI Lab 的专项训练分类，不把内容包装成独立官方试卷。</p><div className="hub-list">{items.map(([id,title,count])=><button className="hub-row" key={id} onClick={()=>go(id)}><span><b>{title}</b><small>{count} · 选择对应训练形态</small></span><strong>→</strong></button>)}</div></div></div>}
