import React from 'react';import Sidebar from './Sidebar';import Header from './Header';import Badge from '../ui/Badge';import useCurrentUser from '../../hooks/useCurrentUser';
export default function DashboardLayout({children,active,onNavigate,route}){const{profile,daysUntilExam,countdownText,directionLabel}=useCurrentUser();return <div className="app-shell"><Sidebar active={active} onNavigate={onNavigate}/><main className="main"><Header route={route}/><div className={active==='today'?'content-grid today-content-grid':'content-grid'}><section className="page-content">{children}</section>{active!=='today'&&<aside className="dashboard-rail"><section className="rail-card exam-card" aria-label="考试倒计时"><p className="eyebrow">EXAM COUNTDOWN</p>{daysUntilExam===null?<><p>未设置考试日期</p><button className="rail-link" onClick={()=>onNavigate('settings')}>设置考试日期 →</button></>:<><div className="exam-value" data-testid="exam-value" aria-label={countdownText}>{daysUntilExam>0?<>{daysUntilExam}<small>天</small></>:daysUntilExam===0?<small>今天考试</small>:<small>{countdownText}</small>}</div><p className="exam-date" data-testid="exam-date">{profile.targetExam.examDate.replaceAll('-', '.')}</p></>}</section>{active!=='analysis'&&<section className="rail-card goal-card" aria-label="今日学习目标"><p className="eyebrow">TODAY'S GOAL</p><div className="goal-value" data-testid="daily-goal">{profile?<>{profile.dailyStudyMinutes} <small>min</small></>:<small>尚未设置学习目标</small>}</div><Badge data-testid="direction-badge">{directionLabel}</Badge></section>}</aside>}</div></main></div>}
