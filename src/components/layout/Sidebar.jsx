import React from 'react';import useCurrentUser from '../../hooks/useCurrentUser';
export default function Sidebar({active,onNavigate}){const{profile}=useCurrentUser();const items=[['today','首页'],['training','训练'],['questions','题库'],['records','学习'],['settings','设置']];return <aside className="sidebar"><div className="brand">CATTI <span>LAB</span></div><p className="eyebrow">ENGLISH · LEVEL 2</p><nav>{items.map(([id,label],i)=><button className={active===id?'nav-item active':'nav-item'} aria-current={active===id?'page':undefined} onClick={()=>onNavigate(id)} key={id}><span><b className="nav-index">0{i+1}</b>{label}</span><small>→</small></button>)}</nav><div className="sidebar-foot"><div className="sidebar-user"><span className="avatar small" style={{flexShrink:0}} aria-hidden="true">{Array.from(profile?.nickname??'L')[0].toUpperCase()}</span><span data-testid="sidebar-nickname" style={{overflowWrap:'anywhere',minWidth:0}}>{profile?.nickname??'学习者'}</span><button className="settings-mark" aria-label="打开设置" onClick={()=>onNavigate('settings')} style={{border:0,background:'transparent',cursor:'pointer',color:'inherit'}}>⚙</button></div><p>Keep a clear record.<br/>Make each sentence count.</p></div></aside>}
