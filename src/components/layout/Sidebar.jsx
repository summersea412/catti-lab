import React from 'react'; import {routes} from '../../app/routes'; export default function Sidebar({active,onNavigate}){return <aside className="sidebar"><div className="brand">CATTI <span>LAB</span></div><p className="eyebrow">ENGLISH ? LEVEL 2</p><nav>{routes.map(r=><button className={active===r.id?'nav-item active':'nav-item'} onClick={()=>onNavigate(r.id)} key={r.id}><span>{r.label}</span><small>↗</small></button>)}</nav><div className="sidebar-foot">Keep a clear record.<br/>Make each sentence count.</div></aside>}
